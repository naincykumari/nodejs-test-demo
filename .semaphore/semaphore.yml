version: v1.0
name: Node.js CI

blocks:
  - name: Build & Test
    task:
      prologue:
        - checkout
      jobs:
        - name: Install dependencies and run tests
          commands:
            - run: npm install
            - run: npm test

